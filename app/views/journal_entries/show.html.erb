<% title(@journal_entry.note, "Journal Entries") %>
<h2><%= @journal_entry.recorded_on %> || <%= @journal_entry.starting_quarter %> || <%= @journal_entry.duration%> || <%= @journal_entry.note%></h2>

<%= link_to "Edit Journal Entry", edit_journal_entry_path(@journal_entry) %>
<%= link_to "Delete Journal Entry", @journal_entry, :method => :delete,
:confirm => "Are you sure you want to delete this journal entry?" %>
